version: "3"
services:
    web:
      build:
        context: web
      restart: unless-stopped

    nginx:
      image: nginx:1-alpine
      ports:
        - "80:80"
      depends_on:
        - web
      volumes:
        - "./server/nginx.conf:/etc/nginx/nginx.conf:ro"
      restart: unless-stopped
